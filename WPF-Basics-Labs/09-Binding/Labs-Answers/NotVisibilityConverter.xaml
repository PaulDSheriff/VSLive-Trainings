<Window x:Class="SimpleDataBindingSamples.NotVisiblityConverter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleDataBindingSamples"
        xmlns:convert="clr-namespace:SimpleDataBindingSamples.Converters"
        mc:Ignorable="d"
        Title="Not Boolean to Visibility"
        WindowStartupLocation="CenterScreen"
        Width="800"
        Height="600">
  <Window.Resources>
    <convert:NotBooleanToVisiblityConverter x:Key="NotBoolToVisibility" />
  </Window.Resources>
  <Border BorderBrush="Black"
          BorderThickness="2"
          Margin="10">
    <StackPanel Margin="10">
      <CheckBox Content="Has Benefits?"
                Name="HasBenefits" />
      <CheckBox Content="401k"
                Visibility="{Binding Path=IsChecked, ElementName=HasBenefits, Converter={StaticResource NotBoolToVisibility}}" />
      <CheckBox Content="Health Care"
                Visibility="{Binding Path=IsChecked, ElementName=HasBenefits, Converter={StaticResource NotBoolToVisibility}}" />
    </StackPanel>
  </Border>
</Window>
