<Window x:Class="SimpleDataBindingSamples.IsEnabledBinding"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleDataBindingSamples"
        mc:Ignorable="d"
        Title="Bind to IsEnabled Property"
        WindowStartupLocation="CenterScreen"
        Width="800"
        Height="600">
  <Border BorderBrush="Black"
          BorderThickness="2"
          Margin="10">
    <StackPanel Margin="10">
      <CheckBox Content="Has Benefits?"
                Name="HasBenefits" />
      <CheckBox Content="401k"
                IsEnabled="{Binding Path=IsChecked, ElementName=HasBenefits}" />
      <CheckBox Content="Health Care"
                IsEnabled="{Binding Path=IsChecked, ElementName=HasBenefits}" />

    </StackPanel>
  </Border>
</Window>
